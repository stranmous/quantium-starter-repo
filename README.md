# Quantium starter repo

This repo contains everything you need to get started on the program! Good luck!

## Dash visualiser

Run the app from the project root with:

```powershell
python app.py
```

The app reads the generated sales output in `data/sales_output.csv` and shows Pink Morsel sales over time with a marker for the 15 January 2021 price increase.
